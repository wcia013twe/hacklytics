# FastAPI Backend Dependencies for Temporal RAG System
# Based on RAG.MD architecture and existing codebase analysis

# Web Framework & API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Data Validation & Models
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Document Processing
pypdf>=4.0.0

# Embedding & ML
sentence-transformers>=2.2.0
torch>=2.0.0
numpy>=1.24.0

# Temporal LLM (local Ollama — run: ollama pull llama3.2:1b)
# No extra Python package needed; uses httpx (already listed below)

# Redis Caching (multi-layer cache for RAG pipeline)
redis>=5.0.0

# ZeroMQ Transport Layer
pyzmq>=25.0.0

# WebSocket Support (for dashboard)
websockets>=12.0

# Database - Actian Vector DB
# psycopg2-binary>=2.9.0  # PostgreSQL driver (Actian Vector compatible)
asyncpg>=0.29.0  # Async PostgreSQL driver

# Vector Operations
scikit-learn>=1.3.0  # For cosine similarity utilities if needed

# Async & Concurrency
aiohttp>=3.9.0
httpx>=0.25.0

# Logging & Monitoring
python-json-logger>=2.0.0

# Environment Configuration
python-dotenv>=1.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.25.0  # For testing FastAPI endpoints

# Development Tools
black>=23.0.0
ruff>=0.1.0
